# Terraform deployment scaffold

This directory contains the starting point for future AWS deployment configuration.

Structure:

- `modules/` for reusable Terraform modules
- `environments/dev`, `staging`, `prod` for environment-specific configuration
